import styled from "styled-components"

const Title = styled.h1`
  font-size: 4.5rem;
  font-weight: 500;
  color: #ffffff;
  text-align: left;
  margin: 0;
  padding: 0;
`;

const High = styled.span`
  color: #6b7ae5;
  stroke: #ffffff;
  text-shadow: -3px 0.6px 0 #1d1c3e;
`;

const TitleDiv = styled.div`
  width: 40rem;
  padding: 6vh 0 0 6vh;
`;

const HeroSection = styled.div``;

export { Title, High, TitleDiv, HeroSection }